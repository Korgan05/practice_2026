# Database (PostgreSQL в Docker, проброшенный порт 5433)
DATABASE_URL=postgresql+psycopg2://practice:practice@localhost:5433/practice_2026

# SMTP (MailHog)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@practice2026.local
SMTP_USE_TLS=false

# Frontend (для ссылки подтверждения в письме)
FRONTEND_URL=http://localhost:5173

# Срок жизни токена подтверждения, часов
EMAIL_TOKEN_TTL_HOURS=24

# JWT (для авторизации) — в проде задать длинный случайный секрет
SECRET_KEY=CHANGE_ME_dev_secret_key_practice_2026
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=720
